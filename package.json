{
  "name": "typed-voice-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server-main.mjs --host 0.0.0.0 --port 3000",
    "test": "node --test test/*.test.mjs"
  }
}
